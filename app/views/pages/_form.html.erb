<%= form_with model: @user, url: users_path, :html => { :multipart => true } do |f| %>
  <div class="form-group">
    <%= f.label :email %>
    <%= f.text_field :email, class: 'form-control' %>
    <%= f.submit "Create", class: 'btn btn-primary' %>
  </div>
<% end %>
